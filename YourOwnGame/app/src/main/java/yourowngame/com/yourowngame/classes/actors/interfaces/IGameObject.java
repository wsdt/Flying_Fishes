package yourowngame.com.yourowngame.classes.actors.interfaces;

public interface IGameObject {
    /** Subclasses might overwrite these values. */
    interface PROPERTIES {
        interface DEFAULT {
            int DEFAULT_ROTATION = 0;
        }
    }
}
